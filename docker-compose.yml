version: "3.5"

services:
  onedental_api_manage:
    image: onedental:1.0
    build: .
    ports:
      - 5000:5000
    networks:
      - onedental_api_manage

networks:
  onedental_api_manage:
    driver: bridge
